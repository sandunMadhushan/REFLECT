<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.REFLECT" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/colorBackground</item>

        <!-- Semantic theme colours used throughout the app -->
        <item name="colorBackground">@color/colorBackground</item>
        <item name="colorCard">@color/colorCard</item>
        <item name="colorTextPrimary">@color/colorTextPrimary</item>
        <item name="colorTextSecondary">@color/colorTextSecondary</item>
        <item name="colorBarInactive">@color/colorBarInactive</item>
        <item name="colorCircleGreen">@color/colorCircleGreen</item>
        <item name="colorCircleBlue">@color/colorCircleBlue</item>
        <item name="colorCirclePurple">@color/colorCirclePurple</item>
        <item name="colorNavBar">@color/colorNavBar</item>
        <item name="colorNotifBtn">@color/colorNotifBtn</item>
        <item name="colorGreenIcon">@color/colorGreenIcon</item>
        <item name="colorBlueIcon">@color/colorBlueIcon</item>
        <item name="colorPurpleIcon">@color/colorPurpleIcon</item>
        <item name="colorProgressTrack">@color/colorProgressTrack</item>
    </style>

    <style name="Theme.REFLECT" parent="Base.Theme.REFLECT" />

    <!-- Splash Screen Theme: fullscreen gradient, no action bar -->
    <style name="Theme.REFLECT.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/bg_splash_gradient</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">#7A5CFF</item>
    </style>
</resources>
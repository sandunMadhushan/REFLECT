<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.REFLECT" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/background_light</item>
    </style>

    <style name="Theme.REFLECT" parent="Base.Theme.REFLECT" />

    <!-- Splash Screen Theme: fullscreen gradient, no action bar -->
    <style name="Theme.REFLECT.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/splash_screen_preview</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">#7A5CFF</item>
    </style>
</resources>